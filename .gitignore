.DS_Store
Thumbs.db

# Phase 3 — secrets locais (chaves SSH/GPG, vault decifrado)
/secrets/
infra/ansible/inventories/*/group_vars/**/vault.yml
!infra/ansible/inventories/*/group_vars/**/vault.yml.example
infra/ansible/.vault-pass
infra/ansible/collections/

# Editor and local environment files
.idea/
.vscode/
*.swp
*.swo
*~
*.tmp
*.bak
*.log

# Dependencies and build outputs
node_modules/
dist/
build/
coverage/
.turbo/
.next/
.cache/

# Local secrets
.env
.env.*
!.env.example
# .env.testing — config de testes versionada (DB_DATABASE=defonline_test, etc).
# Sem segredo: APP_KEY é literal compartilhado, sem credencial de produção.
!.env.testing
# .env.dusk.local — config do Laravel Dusk (DUSK_DRIVER_URL, APP_URL). Mesma
# justificativa: APP_KEY local + sem segredo.
!.env.dusk.local
