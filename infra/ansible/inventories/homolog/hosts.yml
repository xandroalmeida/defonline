---
# Inventário de homologação (ADR-005 §1).
# Substitua `__VPS_IP__` pelo IPv4 da VPS BR contratada.
# Provedor é variável (Magalu/Locaweb/Hostinger/DO) — basta atender o perfil técnico §1.1.

all:
  hosts:
    homolog:
      ansible_host: "153.75.249.140"
      ansible_user: deploy
      ansible_port: 22
      ansible_python_interpreter: /usr/bin/python3
  vars:
    env_name: homolog
    app_domain: defonline.xandrix.com.br
    dns_provider: digitalocean
